# UNA Brain Blocker Remediation Bundle V0.1

**Status:** `PREPARED_READ_ONLY_NOT_CLEARED`

**Authorship:** Codex, assisting/build-time co-developer; not UNA-1 or UNA-2.

**Observed audit status:** `HOLD_BRAIN_AUDIT`

This bundle prepares the next evidence in the required order. It does not alter runtime state,
activate a route, issue independent review, promote memory, or expand authority.

## Current evidence snapshot

The fresh read-only audit observed:

- finding count: `4`;
- candidate assembly: `HOLD_CANDIDATE_ASSEMBLY` with one component hash drift for
  `una_primary_cognition.py`;
- cognitive fitness: `HOLD_INDEPENDENT_SEALED_HOLDOUT_REQUIRED`;
- body/runtime freshness: three stale nodes, including primary cognition, canonical memory runtime,
  and QBox;
- runtime provenance: `HOLD_PROVENANCE_INDEPENDENT_EGRESS_OBSERVER_MISSING`;
- memory: `OPERATIONAL`, eight records, packet bindings present;
- audit boundaries: files written, runtime activated, memory promoted, network attempted, and
  independent review issued all remained `false`.

These are observed local audit results, not activation or certification.

## Work order

### 1. Assembly reconciliation

Prepare a frozen reconciliation candidate for the declared Brain component set. It must identify
the exact candidate fileset, expected and observed hashes, source-state classification, and the
reason for the `una_primary_cognition.py` drift.

Release evidence:

- frozen manifest;
- exact hash comparison;
- independently reviewed reconciliation disposition;
- no manifest rebind performed by the audit itself.

Stop condition: any unexplained drift, dirty ownership ambiguity, or scope expansion is `HOLD`.

### 2. Independent semantic holdout

Prepare an author-disjoint sealed holdout package for semantic fitness. The package must freeze
the candidate, rubric, fixtures, expected outputs, evaluator identity, and review boundary before
evaluation.

Release evidence:

- sealed holdout manifest;
- candidate and rubric hashes;
- distinct reviewer configuration;
- explicit PASS/HOLD disposition.

Self-assessment cannot satisfy this gate.

### 3. Fresh runtime/body observations

Request a fresh, hash-bound, read-only observation snapshot for the stale primary-cognition,
canonical-memory-runtime, and QBox nodes. Freshness must be distinguished from health; unavailable
observations remain unavailable rather than being inferred healthy.

Release evidence:

- observation timestamp and source identity;
- source hashes or equivalent custody evidence;
- freshness and health fields for every required node;
- no runtime restart or activation as part of observation.

### 4. Independent egress observation

Prepare an observer-disjoint provenance check that can verify the declared network/egress boundary
from a distinct approved observer context.

Release evidence:

- distinct observer identity/configuration;
- frozen observation contract;
- passing or holding observer receipt;
- explicit statement that observation does not grant authority.

## Completion rule

After all four evidence packages exist, rerun the read-only Brain audit. Only a fresh audit with no
remaining findings can move the Brain audit status away from `HOLD_BRAIN_AUDIT`. None of these
preparations authorizes activation, canonical-memory promotion, authenticated authorship, external
action, production clearance, or public claims.
